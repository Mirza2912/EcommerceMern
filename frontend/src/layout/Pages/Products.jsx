import React from "react";

const Products = () => {
  return <div>Products</div>;
};

export default Products;
{
  /* <div className="full-div bg-red-900 flex   slg:w-[80%] xsm:w-[80%]  sm:w-[75%] md:w-[85%]  xlg:w-[80%]">
  <div className="image-section w-[550px]  flex items-center justify-center gap-0 px-10 ">
    <div className="image-section w-[100%]  order-1 flex flex-col gap-5">
      {images &&
        images.map((image) => {
          return (
            <figure
              key={image._id}
              className="w-[120px]  h-[120px] cursor-pointer object-center object-cover "
            >
              <img
                src={image.url}
                alt={`${image.name}`}
                className="w-[100%] "
              />
            </figure>
          );
        })}
    </div>

    <div className="big-image order-2 w-[600px]   ">
      <figure className="w-[100%]  object-center object-cover h-[100%] ">
        <img
          className="w-[100%] h-[100%]"
          src={images && images[0].url}
          alt={images && images[0].name}
        />
      </figure>
    </div>
  </div>


  <div className="bg-red-700 w-[100%]">
    <h2>{name}</h2>
    <h2>{category}</h2>
    <div className="w-[100%]  flex items-center gap-1 ">
      <Rating name="read-only" value={rating} readOnly precision={0.5} />
      <h2> {` (${numOfReviews} Reviews)`}</h2>
    </div>
    <h2>{price}</h2>
    <h2>{description}</h2>
    <h2>{stock && stock == 0 ? `Not Available` : `In Stock ${stock}`}</h2>
  </div>
</div>; */
}
